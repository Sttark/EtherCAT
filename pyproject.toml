[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "ethercat-driver"
version = "0.1.0"
description = "Rust-first EtherCAT driver/runtime for the IgH master"
requires-python = ">=3.9"

[tool.maturin]
module-name = "_ethercat_rs"
